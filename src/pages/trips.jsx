import React from 'react';
import Layout from '../components/Layout';

const Trips = () => {
    return (
        <Layout>
            this is trips page
        </Layout>
    );
}

export default Trips;